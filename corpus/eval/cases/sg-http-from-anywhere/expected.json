{
  "description": "Security group ingress on 80/tcp from 0.0.0.0/0",
  "category": "network-exposure",
  "expected": [
    {
      "source": "checkov",
      "rule_id": "CKV_AWS_260"
    }
  ],
  "note": "checkov only. tfsec's aws-ec2-no-public-ingress-sgr fired on any port; Trivy's AWS-0107 (the same check, narrowed upstream) fires only on SSH and RDP ports -- net.is_ssh_or_rdp_port in trivy-checks. Found 2026-09-13 when the two PugetScope findings that motivated context-agent (port 80 and the NodePort range open to the world) stopped being admitted after the Trivy swap. CKV_AWS_260 has no candidate control: CIS 5.2 is about remote-administration ports, and port 80 open to the world is what a public web tier looks like, so it is unmapped rather than force-mapped."
}
