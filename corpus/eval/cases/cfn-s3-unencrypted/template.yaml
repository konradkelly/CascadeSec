AWSTemplateFormatVersion: '2010-09-09'
Description: S3 bucket with no encryption, versioning or access logging

Resources:
  DataBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${AWS::StackName}-data'
